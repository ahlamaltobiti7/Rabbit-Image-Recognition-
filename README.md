# Rabbit Image Recognition using Teachable Machine

## Project Overview

This project demonstrates an image classification model built using **Google Teachable Machine**. The model was trained to recognize two different classes of rabbits:

- Black Rabbit
- White Rabbit

After training, the model was exported in **TensorFlow (Keras)** format and tested using a Python script in **Google Colab** to classify a new input image.

---

## Objectives

- Build an image recognition model using Google Teachable Machine.
- Train the model with two rabbit image classes.
- Export the trained model in TensorFlow (Keras) format.
- Load the model in Python.
- Predict the class of a new image using Google Colab.

---

## Tools Used

- Google Teachable Machine
- TensorFlow (Keras)
- Python
- Google Colab

---

## Dataset

The dataset consists of two image classes:

- **Black Rabbit**
- **White Rabbit**

Multiple images were collected and uploaded for each class to train the model.

---

## Project Workflow

### 1. Dataset Preparation
Two image classes were created in Google Teachable Machine, and training images were uploaded for each class.

### 2. Model Training
The model was trained using the uploaded images until the training process was completed successfully.

### 3. Model Export
The trained model was exported in **TensorFlow (Keras)** format, generating the following files:

- `keras_model.h5`
- `labels.txt`

### 4. Model Testing
The exported model was uploaded to Google Colab. A Python script was used to:

- Load the trained model.
- Read an input image.
- Preprocess the image.
- Predict the image class.
- Display the prediction result.

---

## Model Evaluation

The trained model was tested using a rabbit image. In the demonstrated example, the model correctly classified the image as **White Rabbit** with **100% confidence**.

---

## Project Files

- `rabbit_image_recognition.py`
- `keras_model.h5`
- `labels.txt`
- `images.jpg`
- `demo.mp4`

---

# Results

## Teachable Machine Output

The following image shows the prediction result generated by Google Teachable Machine.

![Teachable Machine Output](images/teachable_machine.png)

---

## Google Colab Demonstration

A demonstration video showing the Python script running in Google Colab and predicting the rabbit image is included in this repository.

**File:** `demo.mp4`

---

## Conclusion

This project successfully demonstrates how to build a simple image classification model using Google Teachable Machine and TensorFlow (Keras). The trained model was exported, tested in Google Colab, and accurately classified rabbit images. The project provides a simple workflow for creating and deploying an image recognition model.

---

## What I Learned

Through this project, I learned how to:

- Build an image classification model using Google Teachable Machine.
- Train a model with multiple image classes.
- Export a trained model in TensorFlow (Keras) format.
- Use Python to load and test the trained model.
- Perform image prediction in Google Colab.
- Document and organize a machine learning project using GitHub.
